<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.iessochoa.joelsemperedura.practica5">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <protected-broadcast
        android:name="android.intent.action.MEDIA_MOUNTED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_nuevo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_nuevo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica5.NoActionBar">

        <activity
            android:name=".ui.PreferenciasActivity"
            android:exported="false"
            android:label="@string/title_activity_preferencias" />
        <activity
            android:name=".ui.EdicionDiaActivity"
            android:exported="true"/>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>